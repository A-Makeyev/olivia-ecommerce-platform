<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
        <title>Verify your Ecom Seller Account</title>
        <!--[if mso]>
        <noscript>
            <xml>
                <o:OfficeDocumentSettings>
                    <o:PixelsPerInch>96</o:PixelsPerInch>
                </o:OfficeDocumentSettings>
            </xml>
        </noscript>
        <![endif]-->
        <style type="text/css">
            * {
                box-sizing: border-box;
            }
            body {
                margin: 0;
                padding: 0;
                min-width: 100%;
                font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
                    "Helvetica Neue", Arial, sans-serif;
                font-size: 16px;
                line-height: 1.6;
                background-color: #f1f5f9;
                color: #334155;
                -webkit-font-smoothing: antialiased;
            }
            a {
                color: #2563eb;
                text-decoration: none;
                font-weight: 500;
            }
            a:hover {
                text-decoration: underline;
            }
            h1 {
                font-size: 22px;
                font-weight: 600;
                letter-spacing: -0.02em;
                margin: 0 0 6px 0;
                color: #ffffff;
            }
            h2 {
                font-size: 28px;
                font-weight: 700;
                letter-spacing: 0.15em;
                margin: 0;
                font-variant-numeric: tabular-nums;
            }
            p {
                margin: 0 0 20px 0;
                color: #475569;
            }
            p:last-child {
                margin-bottom: 0;
            }
            .wrapper {
                width: 100%;
                max-width: 560px;
                margin: 0 auto;
                padding: 40px 20px;
            }
            .card {
                background: #ffffff;
                border-radius: 12px;
                overflow: hidden;
                box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.07),
                    0 2px 4px -2px rgba(0, 0, 0, 0.05);
            }
            .header {
                background: linear-gradient(135deg, #1e40af 0%, #2563eb 100%);
                padding: 32px 40px;
                text-align: center;
            }
            .header-tagline {
                font-size: 13px;
                color: rgba(255, 255, 255, 0.85);
                margin: 0;
                letter-spacing: 0.05em;
                text-transform: uppercase;
            }
            .body {
                padding: 40px;
            }
            .greeting {
                font-size: 18px;
                font-weight: 600;
                color: #0f172a;
                margin-bottom: 24px;
            }
            .otp-box {
                background: #f8fafc;
                border: 2px dashed #cbd5e1;
                border-radius: 10px;
                padding: 24px 32px;
                text-align: center;
                margin: 28px 0;
            }
            .otp-label {
                font-size: 12px;
                color: #64748b;
                text-transform: uppercase;
                letter-spacing: 0.1em;
                margin-bottom: 8px;
            }
            .otp-value {
                font-size: 32px;
                font-weight: 700;
                letter-spacing: 0.2em;
                color: #1e40af;
                font-variant-numeric: tabular-nums;
            }
            .expiry {
                font-size: 14px;
                color: #64748b;
                margin-top: 24px;
            }
            .disclaimer {
                font-size: 13px;
                color: #94a3b8;
                margin-top: 28px;
                padding-top: 24px;
                border-top: 1px solid #e2e8f0;
            }
            .footer {
                background: #f8fafc;
                padding: 24px 40px;
                text-align: center;
            }
            .footer p {
                font-size: 13px;
                color: #64748b;
                margin: 0;
            }
            .footer a {
                color: #1e40af;
            }
        </style>
    </head>
    <body>
        <div class="wrapper">
            <div class="card">
                <div class="header">
                    <p class="header-tagline">Seller verification</p>
                    <h1>Welcome to Ecom Seller Central</h1>
                </div>
                <div class="body">
                    <p class="greeting">Hello <%= name %>,</p>
                    <p>To activate your seller account use the verification code below:</p>
                    <div class="otp-box">
                        <h2 class="otp-value"><%= otp %></h2>
                    </div>
                    <p class="expiry">
                        Enter this code on the activation page within the next
                        <strong>5 minutes</strong>.
                    </p>
                    <p class="disclaimer">
                        If you didn’t apply for an Ecom seller account, you can ignore this email.
                    </p>
                </div>
                <div class="footer">
                    <p>
                        Questions? Contact us at
                        <a href="mailto:support@ecom.com">support@ecom.com</a>
                    </p>
                </div>
            </div>
        </div>
    </body>
</html>
